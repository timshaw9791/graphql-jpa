spring:
  datasource:
     driver-class-name: com.mysql.jdbc.Driver
     username: root
     password: wuliancan
     url: jdbc:mysql://127.0.0.1:3306/mlshop?useSSL=false&characterEncoding=utf-8
  jpa:
     show-sql: true
     hibernate:
       ddl-auto: update

logging:
  level: info
  level.org.hibernate.type.descriptor.sql: trace
  level.org.hibernate.SQL: debug
  #level.org.hibernate.loader: debug